# TODO

## Installer

- Check if Java is installed  
  If not check if installer is present
    If present start it
    If not, open website
- Check if Ollama is installed  
  If not check if installer is present
    If present start it
    If not, open website
- Check which LLMs are available for installation and which are installed
  Offer to install LLMs
  Or open website